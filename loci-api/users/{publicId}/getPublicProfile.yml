info:
  name: getPublicProfile
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/users/:publicId"
  params:
    - name: publicId
      value: "{{user_public_id_1}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/users/:publicId"
      method: GET
      params:
        - name: publicId
          value: ""
          type: path
    response:
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "userId": "",
            "emailAddress": "",
            "fullname": "",
            "username": "",
            "profilePictureUrl": "",
            "memberSince": "",
            "createdAt": "",
            "connectionStatus": ""
          }
